# Security and privacy

Relay v0.1 is local-only. It must not persist source bodies, raw diffs, chat
transcripts, telemetry, or raw command output. Paths, branch names, command
metadata, annotations, and diagnostics are treated as potentially sensitive.

Report security issues privately to the repository owner once the public remote
is created; do not include credentials or customer data in an issue.
