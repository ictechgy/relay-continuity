# Relay v0.1 implementation brief

Implement the approved local-first Relay plan. Non-negotiable invariants: no source/chat/raw-diff/telemetry persistence; deterministic derived card; stale-first assertions; generic core works without adapters; no cloud/team scope.
